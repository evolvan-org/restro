# Docker Assets

Placeholder for infrastructure and container assets.

The current Sprint 0 bootstrap runs entirely with `yarn dev` and needs no
containers. Docker Compose and service images (PostgreSQL, API, web) will be
added here when the database and deployment work begins in a later sprint.
