/**
 * @rms/permissions — single source of truth for roles and permissions.
 * Shared between the API (authorization guards) and the web app (UI gating).
 */

export * from './roles';
export * from './permissions';
export * from './policy';
