/**
 * @rms/db — Prisma client and generated types for the Restaurant Management System.
 * Import the shared client from here rather than instantiating PrismaClient
 * in individual apps.
 */

export * from '@prisma/client';
export { PrismaClient } from '@prisma/client';
